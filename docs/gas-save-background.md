# Быстрое сохранение показания газа

Статус: развёрнуто в `stage9/gas-save-background`, ожидает приёмки владельца.

## Причина и контракт

10 сентября 2026 два PUT показания 1929 м³ за 9 сентября получили HTTP 504:
запись уже была сохранена, но обработчик ждал полной публикации архива.
Браузер пытался разобрать HTML ошибки как JSON. Повтор не создал дубль.

Газовый PUT теперь подтверждает durable-запись без публикации и без ожидания
`.publication.lock`. Ответ содержит актуальные показание, историю и проверку
правдоподобия; форма сразу обновляется. Сохранение, исправление, повтор и удаление
имеют одинаковый контракт. Ошибки HTTP без JSON отображаются понятным сообщением.

HTML и газовая аналитика обновляются существующим worker в ближайшем успешном
цикле. Полный проход остаётся нужен для калибровки и сравнений, затрагивающих другие
периоды, но отдельный проход на каждый PUT больше не запускается. Новые потоки,
очереди и миграции не нужны: каждый цикл читает актуальное состояние БД. После
перезапуска или ошибки публикации следующий успешный цикл подхватывает изменения;
при ошибке синхронизации обновление HTML также задерживается. Форма после reload
читает показание через GET и не зависит от возраста HTML.

Публикация сохраняет существующую блокировку писателей и защиту новых версий.
Профиль, тарифы и комментарии в этой доработке не меняют порядок публикации.

## Проверка

Регрессионный HTTP-тест держит блокировку публикации и проверяет быстрые запись,
повтор, исправление и удаление. Затем новый runtime имитирует перезапуск и ошибку
публикации: повторный проход публикует последнее значение без потери записи.
Клиентские проверки покрывают HTML 504, JSON-ошибку и успешные операции.

## Доказательства выпуска 10 сентября 2026

- Commit `4545ee2`, тег `release-9-gas-save-background-20260910`.
- Образ `ghcr.io/tupoybot/zontanalyzer@sha256:c68b355b267b2ad29d9a0ced7cf5fa880a7f3f437dd062a862b2246c5c9c6530`.
- `deploy/check-local.sh`: 601 тест, Ruff, mypy, wheel/sdist прошли.
  Браузерные сценарии, Basic Auth, конфигурации Compose, ссылки и diff проверены.
  [CI](https://github.com/tupoybot/ZontAnalyzer/actions/runs/34451649962) зелёный.
- Онлайн-копия: `/opt/zont-analyzer/data/backups/zont-analyzer-20260910T074132546680Z.sqlite3`.
  После скачивания проверен именно immutable-образ без сети на отдельной локальной
  БД и каталоге публикации. При удержании `.publication.lock` PUT вернулся за
  0,023 секунды. Повтор показания 1929 не изменил историю. Новый runtime опубликовал
  146 отчётов; профиль, ручные записи, рекомендации и AI-журнал сохранены.
- Артефакты на devbox: `/home/botkin/artifacts/gas-save-background/`;
  `acceptance-result.json`, `immutable-acceptance.log`, `check-local.log`,
  `browser.log`, `basic-auth.log`, `gas-form.png`, `deploy.log`, `smoke.log`.
  Ручной просмотр локального `acceptance/publish/latest.html`: 1929 м³ за 9 сентября,
  форма и история доступны, ошибочного сообщения нет.
- На HK установлен только проверенный образ, сборок и тестовых записей не было.
  Для снижения нагрузки свежая проверенная копия переиспользована при deployment;
  локальный `release-reuse-backup.sh` отличается от `deploy/release.sh` только
  пропуском повторного backup. Схема не менялась (`d92a10b4c601`), предыдущая
  ссылка на образ сохранена в `.env.previous`.
- Контейнер healthy, HTTP health `ok`, штатный цикл `ok`, `analyzed_dates: []`.
  `latest.html` обновлён worker 07:55:58 UTC и содержит новый клиентский код.
  GET показания возвращает 1929 м³ за 9 сентября с одной исходной записью аудита.
  Nginx/Xray активны, внешний запрос без Basic Auth получает 401.

Merge в `main` выполняется только после явной приёмки владельца.
