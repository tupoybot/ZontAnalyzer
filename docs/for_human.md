# ZontAnalyzer: что требуется от владельца

Обновлено: 2026-09-07. Команды первого запуска находятся в [README](../README.md), навигация по
актуальным документам — в [индексе документации](./README.md). Этапы до 8.5 приняты. Сейчас
идёт этап 9 — опытно-промышленная эксплуатация и доработки. Этап 10 (многокомнатность) и этап
11 (управление) необязательны и отложены до накопления опыта эксплуатации.

Исторические планы, отчёты приёмки и завершённые обсуждения вынесены в [архив](./archive/).
В архив следует обращаться только при острой необходимости проверить историческое решение или
восстановить контекст; рабочие инструкции и планы поддерживаются в актуальном разделе.

Принцип: владелец не переписывает сведения, которые приложение может надёжно получить из ZONT.
Оборудование, датчики, режимы, расписания и уставки сначала определяются первичным обнаружением в режиме только для чтения.

## Для опытно-промышленной эксплуатации

Нужно только:

1. предоставить существующий токен ZONT и контактный e-mail через закрытый файл или переменную окружения;
2. проверить результат `doctor --live` и список объектов, полученный командой `discover`;
3. открыть первый локальный HTML-отчёт и отметить непонятные или неверные выводы;
4. не менять настройки котла только ради теста анализатора.

Секреты нельзя присылать в чат или сохранять в Markdown. Локальные файлы
`.access/zontaccesstoken.json` и `.access/.openai_access_token.txt` исключены из Git и должны
иметь права `0600`.

Ручной `config.yaml` не обязателен. [config.example.yaml](../config.example.yaml) нужен только
для точечных переопределений, когда первичное обнаружение ошиблось или владелец осознанно меняет настройку.

## Что полезно подтвердить во время пилота

Приложение вправе попросить подтверждение только если от него действительно меняется вывод.
Наиболее полезны ответы:

- правильно ли сопоставлены отопительный контур, комнатный датчик и БКН;
- был ли замеченный переход режима ручным;
- был ли в конкретном эпизоде реальный запрос тепла от тёплых полов;
- совпала ли найденная пауза после ГВС с наблюдаемым поведением дома;
- было ли резкое падение температуры БКН обычным водоразбором;
- понятен ли отчёт и какое наблюдение оказалось полезным или ложным.

Модель котла, часовой пояс, список датчиков и режимы не нужно повторять, если ZONT уже вернул их с
высокой уверенностью.

## OpenAI — только после локального отчёта

Если владелец хочет AI-интерпретацию:

1. создать отдельный проект OpenAI с небольшим лимитом и уведомлениями;
2. сохранить ключ API проекта в менеджере паролей;
3. передать его как `OPENAI_API_KEY` или закрытый файл, но не в Git/YAML/образ;
4. явно включить `openai.enabled: true` и сначала выполнить один контролируемый анализ.

OpenAI получает компактные рассчитанные факты, а не сырую телеметрию. Приложение продолжает
собирать данные и строить локальные отчёты без AI.

## Telegram пока не требуется

Провайдер Telegram и команды привязки ещё не реализованы. Создавать бота и токен сейчас не нужно.
К каналу доставки вернёмся только после того, как ежедневный HTML/лог подтвердит пользу.

## Рабочее развёртывание

Приложение уже развёрнуто на HK; текущая версия и отличие от main указаны в
[статусе](./status.md). Процедуры обновления, резервного копирования и отката —
в [инструкции эксплуатации](../deploy/OPERATIONS.md). Место внешней резервной
копии и её восстановление следует проверять в ходе эксплуатации.

Процесс развёртывания не должен перезаписывать пользовательскую конфигурацию
и секреты. Приложение остаётся только для чтения относительно ZONT;
возможное управление отложено в необязательный этап 11 с отдельной спецификацией.
