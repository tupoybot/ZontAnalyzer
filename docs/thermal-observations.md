# Полезные наблюдения о тепловой системе

Согласовано 2026-09-09. Ветка `stage9/thermal-observations` создана после слияния
принятой `stage9/ai-freshness` в `main` (merge `5b436a9`).

## Что обнаружено

В `analyst-v8.2` уже были штатные наблюдения, гипотезы тепловой инерции и малой
нагрузки. Сохранённые отчёты за 7 и 8 сентября преимущественно перечисляли метрики
и ограничения настройки. За 8 сентября AI ошибочно использовал среднее за сутки
и 0% ниже допуска как аргументы против отклонения ниже самой уставки ночью.
Таким образом, механизм есть, но постановка задачи недостаточно конкретна.

## Изменение

`analyst-v8.3` уточняет задачу в существующем промпте и формате ответа:

- замечать полезное штатное поведение, объяснять его живым понятным языком;
- различать ровность, точную уставку и аналитический допуск;
- объяснять последовательность наблюдений по временным окнам, а не только средним;
- связывать комнату, подачу/обратку, горение, модуляцию, ГВС и режим;
- хранить физические объяснения в гипотезах с основаниями и альтернативами;
- не объявлять стяжку прогретой, теплосъём измеренным или автоматику обучающейся
  без подтверждающих данных; не доказывать непрерывную работу средними окна;
- сохранять возможность отсутствия рекомендации и недостаточности данных.

Примеры владельца определяют глубину, а не набор обязательных диагнозов.
Новых локальных классификаторов и шаблонных положительных сообщений нет.
Формулы, модель AI, схема ответа и исторические AI-ответы сохраняются.
Новый промпт действует при следующих AI-анализах; публикация прежнего отчёта
сама по себе не запускает повторный AI-запрос.

Известный дефект среднего превышения из [диагностики допуска](./target-band-note.md)
остаётся открытым. Промпт требует сопоставлять сомнительные расчётные величины
с диапазонами исходных данных; это не исправление формулы и не гарантия фильтрации.

## Проверка

Проверяем существующие контракты пакета, сериализации и публикации, затем один
контролируемый AI-анализ на изолированной копии реальных данных. Рубрика ручной оценки:
есть конкретное полезное наблюдение; эпизоды и цифры соответствуют входу;
положительные выводы ограничены данными; точная уставка не подменена допуском;
причины отделены от наблюдений; нет обязательной истории про стяжку/калибровку
или совета без необходимости. Один ответ не доказывает устойчивость на всех режимах.

Для этой правки промпта не добавляем тесты на наличие отдельных фраз: они не
проверяют качество рассуждения. Подход к версии промпта и проверке результата
согласуется с [OpenAI Docs](https://developers.openai.com/api/docs/guides/prompt-engineering).
Технические результаты и ограничения выпуска записываются после проверки.

### Проверка 2026-09-09

Docker на devbox: Ruff, mypy, **600 тестов**, wheel/sdist прошли; новых тестов нет.
На отдельной записываемой копии online-backup пересчёт 7 и 8 сентября сохраняет
значения метрик, события и качество данных. В пакетах есть текущие окна с ошибкой
температуры ниже точной уставки и явный допуск 0,5 °C; размер около 60 КБ.
Это ограниченная выборка окон, не полный временной ряд и не доказательство
непрерывного режима горения. Исторические пояснения сохраняются при рендеринге.

Выполнена одна попытка реального AI-анализа на локальной копии, модель из настроек
`gpt-5.6-luna`, промпт `analyst-v8.3`. OpenAI вернул HTTP 403
`unsupported_country_region_territory`. Ответ не получен; повторов и обхода
регионального ограничения не было. Новый текст модели и выполнение смысловой
рубрики пока **не проверены**. Моковые ответы не подменяют эту проверку.
Рабочие отчёты и AI-журнал на HK этот запрос не затронул.

Опубликованный образ отдельно прошёл локальную проверку без сети на той же копии:
оба дня сохраняют значения метрик/события/качество, старый AI рендерится без изменения,
пакеты занимают 60371/60375 байт и содержат по шесть сопоставимых окон отопления,
включая текущие отклонения ниже уставки. AI-журнал и ручные записи неизменны.
Результат: `/tmp/za-observations-immutable-accept.log`, диагностические пакеты и HTML
на devbox: `/tmp/za-observations-output/`. Это проверка передачи данных, не качества
нового ответа модели.

CI: [34318756807](https://github.com/tupoybot/ZontAnalyzer/actions/runs/34318756807),
включая браузер, Basic Auth, Compose и публикацию образа, прошёл.
Исходный commit `b7070c3`, тег `release-9-thermal-observations-20260909`.
Образ: `ghcr.io/tupoybot/zontanalyzer@sha256:5d562c6f1cca30257ebe61455756d91a1fef59f9ca93e6462b2cef6c06f809f7`.

HK release `20260909-thermal-observations-b7070c3`: контейнер healthy,
HTTP health `ok`, внутри образа подтверждён `analyst-v8.3`. Worker завершил цикл:
`state: ok`, `last_success_at: 2026-09-09T06:32:24+00:00`, `analyzed_dates: []`.
JSON за 7 и 8 сентября совпали по SHA-256 до/после; сохранены 35 записей AI-журнала,
18 вмешательств и пустая таблица экспериментов. Общая сноска в обоих отчётах и
`latest.html` присутствует один раз. Nginx/Xray активны, без авторизации сайт даёт 401.

Миграции БД нет. Для изолированной приёмки и этого выпуска повторно использован
проверенный online-backup того же дня:
`/opt/zont-analyzer/data/backups/zont-analyzer-20260909T030328793375Z.sqlite3`.
Это сохраняет минимальную нагрузку на HK; полные проверки и попытка AI были на devbox.
Откат: прежний release `20260909-target-band-18b9158` и его сохранённый immutable digest.
Новая ветка ожидает отдельной приёмки, слияния `stage9/thermal-observations` не было.
