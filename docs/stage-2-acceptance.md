# Этап 2 — временные свидетельства отопления и ГВС

Ветка: `stage2`, от актуального `origin/main`. Приёмка выполняется 2026-09-06.
**Этап 2 закрыт 2026-09-06: обе порции, локальная и production-приёмка выполнены.**

## Контракт

- `analytics/evidence.py` формирует отдельный типизированный пакет в
  `Report.context.temporal_evidence`. Схема SQLite и схема ответа AI не меняются.
- Источники обозначены устойчивой комбинацией device/source/entity/metric, названием
  из ZONT, единицей и происхождением. Неоднозначный выбор ряда остаётся unknown.
  Для ΔT используется явно выбранная либо однозначная внешняя обратка; неподтверждённый
  `rwt` адаптера не подставляется вместо внешнего измерения.
- Контрольная комната сопоставляется с историческим заданием контура. Текущее ручное
  предпочтение температуры не переписывает историческое задание в пакете свидетельств.
- Окна сохраняют точные границы UTC, часовой пояс дома, ID, значения и диапазоны,
  изменения и независимое покрытие каждого ряда. Длинный период использует тот же
  ограниченный размер пакета; пропущенная информация отмечается явно.
- Состояние горелки определяется `fl`. Нулевая модуляция при `fl=1` остаётся
  наблюдаемой точкой шкалы активного горения. Профиль
  `analysis.modulation_capability_profile: flame_zero_is_minimum` выбирается только
  при подтверждённой семантике оборудования; default — `unknown`. Пересчёта в кВт нет.
- KPI имеют знаменатель, покрытие и причину недоступности. Приоритет ГВС, летний/выключенный
  режим, переходы задания/режима, ненадёжные интервалы и ранее отфильтрованный шум
  исключаются из сопоставимых расчётов отопления.
- Погодные ступени характеризуются по исходным отсчётам; это не измерение причины
  изменения погоды. Совпадающие изменения температуры комнаты/ГВС и скорость остывания
  доступны в одних временных окнах. Без прямого сигнала рециркуляции работа насоса и
  присутствие людей не объявляются измеренными фактами.
- AI получает пакет в существующем входном контракте. Полный compact JSON ограничен
  64 KiB, исключённые объекты учитываются в `provenance.truncation`. Прежняя схема
  ответа сохранена; ID окон можно указывать в `evidence_event_ids`.
- В HTML/text есть раскрываемые временные свидетельства с именами показателей,
  местными интервалами и пробелами. Старые отчёты без этого контекста совместимы.
  Встроенные графики для этапа не требуются.

## Изолированная приёмка

Online backup создан штатной командой работающего приложения на HK:
`/opt/zont-analyzer/data/backups/zont-analyzer-20260906T124857500628Z.sqlite3`.

Источник скачан в `/tmp/zont-stage2/source/production.sqlite3`;
отдельная записываемая копия — `/tmp/zont-stage2/data/`, публикация —
`/tmp/zont-stage2/publish/`. Тесты, сборки и анализы выполняются только локально.

Результаты проверок, commit/tag/digest и production smoke записаны ниже.
Слияние в `main` выполняется после зафиксированной приёмки.

## Выполненные локальные проверки

- 159 pytest-тестов; Ruff; mypy, 32 исходных файла. Единственный первоначальный сбой
  HTTP-теста был запретом sandbox на AF_INET; разрешённый повтор прошёл.
- Собраны sdist/wheel и runtime/test Docker images; тест HTTP feedback внутри образа,
  nginx Basic Auth и Playwright archive/feedback E2E прошли. Compose валиден.
- Исходный online backup: `integrity_check=ok`, revision `9f4a2c8d1e70`.
- Локальные отчёты: `/tmp/zont-stage2/publish/2026-09-05.{html,json,txt}` и
  `/tmp/zont-stage2/publish/2026-05-08.{html,json,txt}`; пакеты AI рядом, `*-packet.json`.
  Результаты: `/tmp/zont-stage2/acceptance-results.json`.
- На 5 сентября: 1,1429 запуска/ч запроса; медиана/P90 завершённого цикла
  750/984 с; отношение горения к запросу 0,8857; медиана модуляции 0 при активном `fl`.
  Для 8 мая без подходящего запроса требующие его KPI недоступны с причиной.
- Мобильный Chromium, 390 px: горизонтальной прокрутки нет; сохранены
  `/tmp/zont-stage2/{desktop,mobile}.png`. Проверены названия, интервалы, единицы и причины.
- Покрытие KPI — наблюдённое пригодное время / пригодное время, а не доля отопления
  во всём дне. Flow/cs — совместное покрытие только наблюдённого запроса отопления.
  Взвешенные квантили не переоценивают короткие участки с частыми отсчётами.
- Штатная схема Responses Structured Outputs сохранена по
  [официальному контракту](https://developers.openai.com/api/docs/guides/structured-outputs).
  После отдельного явного согласия владельца выполнен один реальный запрос,
  `max_retries=0`: 20604 input / 624 output tokens, `gpt-5.6-luna`, medium.
  Response успешно разобран прежней схемой; пустой список рекомендаций уместен,
  летний режим и прежние решения владельца учтены. Артефакты: `/tmp/zont-stage2/ai-result.json`
  и `/tmp/zont-stage2/publish/2026-09-05-ai.{html,txt}`. Usage вырос ровно на одну запись;
  повторных запросов нет. Первоначальная автоматическая блокировка разрешена явным согласием.

## Публикация и приёмка immutable образа

- Коммит приложения: `4ba9cd3282875fbc8178d352a6f15b1ee82d8cc7`.
- Тег: `release-2-20260906`.
- [Release CI](https://github.com/tupoybot/ZontAnalyzer/actions/runs/34035224385)
  и [branch CI](https://github.com/tupoybot/ZontAnalyzer/actions/runs/34035222672) — success.
- Принятый образ:
  `ghcr.io/tupoybot/zontanalyzer@sha256:7c4596fd9669c9570d4fed3fec81feec4994252bef3ba455ecec3de499b18d40`.
- OCI revision label в опубликованном образе отсутствует; вместо него локально сверены
  SHA-256 всех 32 Python-файлов установленного приложения — совпали с коммитом.
- Этот digest отдельно запущен локально с `--network none`, read-only root filesystem,
  штатным UID и только writable mounts копии БД/одноразовой публикации. Оба реальных
  дневных отчёта рассчитаны, артефакт `/tmp/zont-stage2/registry-acceptance.log`.
- 14 явных решений `applied/rejected` и 14 interventions на локальной копии совпали
  с исходным backup побайтно на уровне SQL-строк; `integrity_check=ok`, FK-нарушений нет.

## Финальная проверка приоритета свидетельств

На реальном ограниченном пакете обнаружено, что полные эпизоды ГВС могли не помещаться
после агрегатов. Добавлена отдельная regression-проверка совместного сохранения эпизодов
и метрик: сначала сохраняется представитель каждого важного вида события, затем
агрегаты и дополнительные эпизоды. Пакет за 5 сентября теперь содержит полный эпизод
ГВС, летний переход, 41 прежнюю метрику и все 12 operational KPI в temporal evidence;
размер 59601 байт, пропуски явно перечислены. HTML/JSON отчёта сохраняет полные метрики.

Корректировка: `1bf426e5ddad52e184a0301645dcd95453f51fb7`, тег
`release-2-20260906-r2`. 160 тестов, Ruff/mypy, sdist/wheel и локальный Docker прошли;
кандидат повторно рассчитан на изолированной копии с отключённой сетью.
API-схема не менялась, повторного OpenAI-вызова не было. Точный ранее отправленный
AI-input сохранён в `/tmp/zont-stage2/ai-input.json`; его SHA-256 совпал с usage-записью:
`af4e84b9a82987283be46a271af127632ac76301e7258e2bb39e88defe6f5226`.

Первый upgrade остановился до замены контейнера: старый бинарный файл не принимал
новое поле `modulation_capability_profile` при `db backup`. Старая конфигурация восстановлена,
штатный `release.sh` выполнен, затем профиль добавлен в уже обновлённую конфигурацию
и перезапущен только worker. Соседние сервисы не изменялись. Проверенный backup:
`/opt/zont-analyzer/data/backups/zont-analyzer-20260906T131741236845Z.sqlite3`.
Для финальной корректировки без миграции этот свежий backup переиспользуется;
повторная тяжёлая копия на HK не требуется. Конфигурация до этапа сохранена в
`/opt/zont-analyzer/config.yaml.before-stage2`, предыдущий digest — в `.env.previous`.

## Финальная production-приёмка

- Финальные [release CI](https://github.com/tupoybot/ZontAnalyzer/actions/runs/34036090733)
  и [branch CI](https://github.com/tupoybot/ZontAnalyzer/actions/runs/34036088173) — success.
- Рабочий digest:
  `ghcr.io/tupoybot/zontanalyzer@sha256:0ef3d79481fed4d325a552ed2ebe19064b6d1444456491b593bccd08b3ddc454`.
  Все 32 Python-файла сверены с `1bf426e`; локальная приёмка именно этого образа:
  `/tmp/zont-stage2/registry-final-acceptance.log`.
- `current`: `/opt/zont-analyzer/releases/20260906-stage2-final-1bf426e`.
  Container started `2026-09-06T13:31:24Z`, healthy, `OOMKilled=false`.
  `last_success_at=2026-09-06T13:32:48Z`, state `ok`, штатно опубликован 2026-09-05.
  Новые свидетельства и подтверждённый профиль есть в свежем `latest.html`.
- Main root и `/api/health` без credentials вернули 401. С временным acceptance
  пользователем root, `/daily/2026-09-05.html`, `/api/health` и legacy `/za/latest.html`
  вернули 200; HTML содержит новый блок. Пользователь удалён в `finally`, исходный
  htpasswd восстановлен побайтно. Пароль владельца не читался и не менялся.
- Содержание, категории, статусы и комментарии всех 14 `applied/rejected` сохранены.
  Все 14 interventions совпали побайтно на уровне SQL-строк. Полный hash строк
  recommendations закономерно изменился: прежний `save_report` при перепубликации
  обновляет `updated_at` трёх рекомендаций за вчера. Проверка отдельно доказала,
  что иных изменений нет; данные владельца не восстанавливались и не перезаписывались.
- nginx, Xray, Postfix, OpenDKIM и Docker active. Их конфигурация не изменялась;
  приложение по-прежнему публикует API только на `127.0.0.1:8787`.
- Smoke артефакты: `/tmp/zont-stage2/{worker-smoke,routes-smoke,feedback-preservation}.json`,
  `/tmp/zont-stage2/services-after.txt`; backup переиспользован, как указано выше.

Сборки, тесты, приёмочные анализы и единственный AI-запрос выполнены локально/в CI.
HK выполнил только доставку готовых образов, штатный backup, deployment и bounded smoke;
аналитический цикл после старта — обычная работа worker, тестовые отчёты в production
не подменялись. Порции 2а и 2б приняты вместе. Закрытие продуктового P2 владельцем
не объявляется результатом этого технического этапа.
