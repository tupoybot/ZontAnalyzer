# Этап 3 — профиль оборудования и ранние показания газа

Ветка `stage3` создана от актуального `main` (`dfc2a6e`). Приёмка: 2026-09-06.
До завершения registry/HK smoke этап не считается закрытым.

## Контракт реализации

- `devices` остаётся read-only. Перед общей редакцией приватных данных извлекаются
  только координаты объекта `stationary_location.loc` (долгота, широта) и
  `z3k_config.boiler_adapters[].boiler_model`. Сохраняются источник и время наблюдения.
  `z3k_config.location` — положение в конфигурации, не географическое поле.
  Неподходящие типы, отсутствующие координаты и неоднозначный выбор адаптера дают unknown.
  Обезличенная структура: `tests/fixtures/zont_contract/equipment.json`.
- Форма профиля необязательна и свёрнута. Источники и история доступны рядом с полями;
  найденные координаты показаны, ручное уточнение открывается отдельно. AutoAdapt,
  гидрострелка и плита различают `null`, `true`, `false`. Мощность кВт хранится отдельно
  от модели; объёмный паспортный расход — в м³/ч с источником, видом газа и применимостью.
- Ручные уточнения имеют приоритет над последующими автонаблюдениями. Возврат к авто —
  отдельная ревизия. Без даты сведения действуют с момента сохранения; введённая дата
  трактуется в часовом поясе дома. В анализ передаются профиль на начало периода и
  изменения внутри него. Обновление формы не запускает анализ или OpenAI.
- Узкие GET/PUT `/equipment/{device_id}` и `/reports/{report_id}/gas` используют
  существующий loopback API и nginx Basic Auth. GET `/equipment` перечисляет профили.
  POST, произвольные поля ZONT, подмена источника/счётчика/даты не поддерживаются.
- День показания определяется сервером по существующему daily report и его timezone.
  `latest` содержит ID отображённого отчёта. Одно действующее показание на день
  установки; версии отчёта за тот же день используют одну запись. Время снятия имеет
  точность «день»; время ввода отдельно, фиктивного измерения в полночь нет.
- Decimal, неотрицательность, границы представления и монотонность внутри участка
  проверяются до записи. Повтор сохраняет timestamps и журнал. Исправление и удаление
  оставляют audit. Замена/сброс/переполнение — отдельная сохраняемая граница учёта,
  включая ввод дней в любом порядке и удаление самого показания на границе.
  Записи сериализуются SQLite `BEGIN IMMEDIATE`.
- Проверка правдоподобия предварительная: без исторически применимого паспорта или
  при неоднозначном оборудовании — unknown. Известный max допускает предупреждение
  с учётом максимальной длительности между локальными днями. Показание сохраняется;
  мощность не переводится в м³/ч. Калибровка, распределение по дням и экономия — этап 8.
- Миграция `9f4a2c8d1e70 → b81c3d4e5f60` добавляет ревизии профиля, показания,
  границы счётчика и audit. Owner feedback и interventions остаются отдельными сущностями.

## Проверка текущего контракта и паспорта

Проверены короткие локальные read-only `devices` запросы; HK для них не использовался.
В рабочей установке найдено семейство `baxi-eco-life` и обе координаты. Значения координат
и секреты в документацию/логи не включены. Общая редакция токенов, адресов и остальных
личных полей сохранена; разрешённое исключение касается только требуемых координат.
Географическая последовательность ZONT — долгота, широта; структура конкретного поля
закреплена live-проверкой и fixtures. [Документация ZONT API](https://zont-online.ru/api/docs/).

Проверено [официальное руководство ECO Life](https://service.baxi.ru/info/download-manual?name=ECO+Life++%D0%BA%D0%BE%D1%82%D0%BB%D1%8B+-+%D0%B8%D0%BD%D1%81%D1%82%D1%80%D1%83%D0%BA%D1%86%D0%B8%D1%8F.pdf),
страница PDF 31: разные варианты мощности семейства имеют разные расходы; G20 задан
в м³/ч, G30 — в кг/ч. Без точного варианта и вида газа паспортные числа не подставлены.
Локальный исходник: `/tmp/zont-stage3/eco-life-manual.pdf`.

## Локальные доказательства

- Свежий штатный online backup HK:
  `/opt/zont-analyzer/data/backups/zont-analyzer-20260906T134737353845Z.sqlite3`.
  Источник: `/tmp/zont-stage3/source/production.sqlite3`; отдельная записываемая копия:
  `/tmp/zont-stage3/data/`; публикация: `/tmp/zont-stage3/publish/`.
- Исходный backup: integrity `ok`, ревизия `9f4a2c8d1e70`, 126 daily / 4 initial отчёта.
  Приёмка после миграции: integrity `ok`, FK ошибок нет, все 14 явных решений владельца
  и 14 interventions сохранены побайтно на уровне SQL-строк. Показаний не выдумывали.
- Пересчитаны 2026-09-05 и 2026-05-08 с `--no-ai`; HTML/JSON/text и ограниченные
  AI-пакеты лежат в `/tmp/zont-stage3/publish/`. Новое оборудование не перенесено в прошлое;
  пакет содержит явный исторический unknown. Результат: `/tmp/zont-stage3/acceptance-results.json`.
- 204 локальных теста, Ruff, mypy (36 исходных файлов) прошли. Сборки sdist/wheel и
  Docker выполнены локально. В браузере проверены root/legacy, архив/`latest`, gas CRUD,
  монотонность, reset, профиль/tristate/auto reset, XSS, feedback и ширина 390 px.
  Артефакты проверок находятся в `/tmp/zont-stage3/`; изображение браузерного прогона:
  `/tmp/zont-stage18-archive-mobile.png`.
- OpenAI-запросов на итерации: **0**. Ввод/исправление профиля или газа не вызывает AI.

## Релиз

Digest, CI, приёмка опубликованного образа и bounded HK smoke будут записаны после
прохождения этих проверок. До этого этап открыт.
