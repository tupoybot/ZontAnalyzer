# Приёмка этапа 1.7

Ветка: `stage-7.1`. Аналитические эпохи и прежние пункты 9–11 исключены по решению владельца.
Именованных границ истории, отдельной ретроспективной команды и изменений уникальности отчётов нет.

## Реализовано

- Автоматический `new → ignored` через 48 часов, при startup и каждом цикле worker.
  Повторный maintenance идемпотентен; явные поздние `applied`/`rejected` сохраняются.
- Нейтральное «Без реакции» в HTML, `ignored` в CLI; AI feedback содержит только явные исходы.
- Миграционный checkpoint `9f4a2c8d1e70` без DDL: существующая БД получает проверенный online backup
  до первого массового изменения. До expiry выводится число затрагиваемых записей.
- Локальные тексты и AI-инструкция очищены от шаблонных сервисных советов; нормальный день может
  не содержать рекомендаций. Сохранённая AI-интерпретация старой политики не возвращается при refresh.
- Bootstrap запрашивает доступный архив по каждому устройству/типу, хранит завершённые части и
  фактические границы/`time_truncated`. `mintime=0` — граница протокола, не дата ремонта дома.
- CI проверяет push/PR; публикация GHCR только по `release-*` или ручному `publish=true`.
  Серверный Compose не содержит `build`; явный `release.sh` использует digest, backup и healthcheck.
  Локальная сборка и отладка имеют отдельный Compose и volume.

## Проверка на копии рабочей БД

Online backup получен штатным `db backup` с HK, проверен `doctor`, затем использована отдельная
локальная записываемая копия. Исходный снимок: 1 088 495 измерений, ревизия `7c8e9f1a2b3c`.

| Статус | До | После maintenance |
| --- | ---: | ---: |
| new | 137 | 6 |
| applied | 7 | 7 |
| rejected | 2 | 2 |
| ignored | 0 | 131 |

Повторный maintenance: 0 изменений. `integrity_check`: `ok`, `foreign_key_check`: без нарушений.
Артефакты проверки: `/tmp/zont-stage17-final-acceptance/{daily,initial}.{html,json,txt}`;
сводка `/tmp/zont-stage17-final-acceptance/acceptance.json`.

Daily за 2026-09-04: качество 87,42%, рекомендаций нет. Initial охватывает сохранённый диапазон
2026-05-04 — 2026-09-04; качество 9,02% честно ограничивает выводы по разреженной истории.
OpenAI при этой проверке отключён.

## Проверка bootstrap и локальной сборки

Большой ответ ZONT обрабатывается потоково, записи поступают в SQLite пакетами по 2 000.
После OOM первоначального варианта исправленный образ продолжил с незавершённого типа:
`exit=0`, `OOMKilled=false`, лимит 384 MiB без swap. Итог: 481 440 измерений, 47 рядов,
189 source events, `complete=true`, `integrity_check=ok`.
Фактически доступный через API диапазон: 2026-07-22T11:45:40Z — 2026-09-04T20:42:00Z.
Пустой `temperature` сохраняет null-границы; у радиоизмерений первая точка 2026-08-23.
У каждого типа сохранены фактические requested bounds и `time_truncated=true`.
Это ограничение текущего архива API, не основание удалять старые точки рабочей БД.

Локально проходят 118 тестов, Ruff, mypy, sdist/wheel build, сборка runtime/test images,
Docker feedback HTTP E2E и валидация обоих Compose. Deployment проверен с fake Docker:
ошибка pull/backup не меняет .env и не запускает контейнер; повторный запуск сохраняет
предыдущий image reference для ручного rollback.

## CI, registry и AI

Коммит приложения: `1bc23926397c4695c4d5e226c427bd76768a8d50`.
Релизный тег: `release-1.7-20260905`.
[CI ветки](https://github.com/tupoybot/ZontAnalyzer/actions/runs/33917896684) и
[CI/publish релиза](https://github.com/tupoybot/ZontAnalyzer/actions/runs/33918116382) прошли успешно.

Опубликованный и полученный через GHCR локально и на HK образ:
`ghcr.io/tupoybot/zontanalyzer@sha256:473c3d484214861622af0377667082a1cdc72aeb5dc55c9bc94ef0e3993daf96`.
На сервере образ не собирался и tar-файл образа не передавался.

Один контрольный AI-вызов на локальной production-копии завершился валидной интерпретацией:
описаны системное поведение и ограничения данных, рекомендаций нет, шаблонных советов
об аварии/специалисте нет. Артефакты: `daily-ai.html` и `daily-ai.json` в каталоге локальной приёмки.

## Изолированная приёмка на HK

Свежий проверенный online backup:
`/opt/zont-analyzer/data/backups/zont-analyzer-20260904T205445820319Z.sqlite3`.
Кандидат использовал отдельную записываемую БД и непубличный каталог
`/opt/zont-analyzer/acceptance/stage17-1bc2392`; рабочие data/publish не монтировались.
Образ получен из GHCR, лимит контейнера — 384 MiB без swap, AI отключён.

Первый цикл завершился в `2026-09-04T21:00:27Z`: все 6 типов bootstrap обработаны,
добавленная история не удалила сохранённые майские точки. После maintenance: 6 `new`,
131 `ignored`, 7 `applied`, 2 `rejected`. Initial добавил одну `observe_only` рекомендацию
о недостаточном качестве истории, поэтому после него `new=7`.

Просмотрены `publish/initial.{html,json}` и `publish/daily/2026-09-04.{html,json}`:
daily — 87,42% качества, рекомендаций нет; initial — 9,04% качества и явное ограничение выводов.
Управляющая температура — радиодатчик «Гостиная», связь подтверждена конфигурацией контура;
техническая температура котельной, влажность и обратка остаются отдельными рядами.

`doctor` успешен; `integrity_check=ok`, `foreign_key_check` без нарушений, ревизия `9f4a2c8d1e70`.
Повторный короткий цикл после bootstrap завершился успешно; health в `21:14:44Z` — `ok`,
возраст heartbeat 6,793 секунды. Повторный maintenance: `eligible=0`, `ignored=0`.
Доказательства сохранены рядом с БД: `doctor.log`, `second-cycle.log`, `health.log`,
`maintenance.log`, `final-check.status` (`checks_ok`).

## Рабочее развёртывание

`deploy/release.sh` завершился с кодом 0. Online backup перед переключением:
`/opt/zont-analyzer/data/backups/zont-analyzer-20260904T211521787707Z.sqlite3`;
проверенный checkpoint перед миграцией: `zont-analyzer-20260904T211622703111Z.sqlite3`
в том же каталоге. Предыдущая `.env` сохранена как `.env.previous`.

`current` указывает на `/opt/zont-analyzer/releases/20260905-stage17-1bc2392`;
предыдущий release-каталог сохранён. Контейнер использует указанный выше digest,
OCI revision совпадает с `1bc23926397c4695c4d5e226c427bd76768a8d50`.
Сборки на сервере и переноса образа/живой БД между средами не было.

Первый рабочий цикл завершился в `2026-09-04T21:20:32Z`: `state=ok`, `sync.complete=true`,
Docker health — `healthy`, `OOMKilled=false`. Штатный `latest.html` обновлён в `21:20:32Z`.
Рабочая БД: `integrity_check=ok`, без нарушений внешних ключей, ревизия `9f4a2c8d1e70`;
6 `new`, 131 `ignored`, 7 `applied`, 2 `rejected`. Повторный maintenance не меняет записей.
Явные решения, их timestamps/комментарии и строки `interventions` совпадают с предрелизной backup.

Внешние `/za/`, `/za/daily/`, `/za/latest.html`, `/za/api/health` возвращают 200;
GET feedback без ключа — 401, с ключом существующая старая рекомендация возвращает `ignored`.
Синтетические записи в рабочую БД не добавлялись. nginx, Xray, Postfix, OpenDKIM и Docker активны.
Лог развёртывания: `deployment.log` и `deployment.exit` в release-каталоге.

**Этап 1.7 закрыт 2026-09-05 (Europe/Samara).** Этапы 1.6/1.8 и продуктовая оценка P2
этой технической приёмкой не закрываются.
