# Выпуск 9.2 — модели AI, 2026-09-08

Реализация и deployment завершены. Владелец явно принял 9.2 командой «ок мерж»
2026-09-08 после уточнения шапки; ветка слита в `main`. Перед merge локально
выполнен `docker system prune -af` (освобождено 538,3 MB); на HK очистка не выполнялась.
Активные контракты: [план 9.2](../model-maintenance-plan.md),
[оценочные пакеты](../model-evaluation.md), [процедура выпуска](../release-process.md).

## Версия и проверки

- Ветка: `stage9/ai-models`; код `f4636b01155e414e3c471d47e386068bc2db7ba6`.
- Тег: `release-9.2-20260908-ai-models-v2`.
- Образ: `ghcr.io/tupoybot/zontanalyzer@sha256:628b8b46a3698107e0ae7ed617eb51dd12f621dfcd7b5d8bced95ba38227539c`.
- [CI релиза](https://github.com/tupoybot/ZontAnalyzer/actions/runs/34207044141)
  и [CI ветки](https://github.com/tupoybot/ZontAnalyzer/actions/runs/34207041259) прошли.
  Резервный запуск публикации отменён после успешных проверок основного.
- `deploy/check-local.sh all`: 593 теста за 82,28 с, Ruff, mypy (80 файлов),
  wheel/sdist; локальная сборка рабочего образа и Compose config прошли.
- Все 80 установленных Python-файлов опубликованного образа сверены с исходниками
  коммита; затем образ принят локально без подмены исходников через bind mount.

## Локальная приёмка на рабочих данных

Online backup скачан с HK: `/opt/zont-analyzer/data/backups/zont-analyzer-20260908T082517693436Z.sqlite3`.
Оригинал хранится сжатым в `/home/botkin/artifacts/stage9.2/private/production.sqlite3.gz`.
Конфигурация подключалась только для чтения; БД и публикация — отдельные локальные.

Опубликованный образ выполнил миграцию `c3d7a8e9f102` → `d92a10b4c601`, публикацию
и полный цикл worker на локальной копии за 65,31 с: 146 сохранённых отчётов,
144 опубликованных; дневных и календарных пересчётов нет. Новые анализы и обращения
к OpenAI в сценарии запрещены моками. Пользовательские таблицы и AI-журнал сверены
по содержимому и не изменились; версии отчётов сохранены.

Отдельный дневной отчёт за 2026-09-07 без AI содержит 41 метрику и 15 событий.
HTML/JSON/текст проверены: сведения об отоплении/ГВС, газе и стоимости сохранены,
происхождение обозначено «Без AI-анализа». Сравнительная генерация не выполнялась.

Браузер проверен с реальным локальным API на 1280/390 px: сохранение и сброс настроек,
источники, подпись модели. Мок-сценарии дополнительно проверяют оба профиля предложений,
целочисленные версии решений и legacy-префикс. Ошибок JavaScript и переполнения нет.

## HK deployment и короткий smoke

- Checkout: `/opt/zont-analyzer/releases/20260908-stage9.2-ai-models-f4636b0`;
  `/opt/zont-analyzer/current` указывает на него. Прежний релиз сохранён для отката.
- `deploy/release.sh` развернул точный проверенный digest без сборок на HK.
  Перед заменой выполнен проверенный online backup:
  `/opt/zont-analyzer/data/backups/zont-analyzer-20260908T091027536255Z.sqlite3`.
- Контейнер healthy, перезапусков 0. Штатный цикл завершился `ok` в 09:15:47 UTC:
  144 публикации, `analyzed_dates=[]`, `long_periods=[]`.
- Все 31 записи `llm_calls`, профиль, показания/тарифы/аудит газа,
  вмешательства и рекомендации совпали с контрольными хешами до deployment.
- Первая фоновая проверка каталога 09:14:09 UTC успешна; официальные страницы HTTP 200.
  Следующая дата 2026-11-07. Предложение `review: terra → luna` показывает цены,
  источники и необходимость оценки качества. Действующие модели luna/terra,
  обе `medium`, AI включён, интервал 60 дней; веб-переопределений нет.
- Защищённый сайт и legacy URL, API и manifest проверены браузером 1280/390 px.
  Подпись AI и настройки видны; ошибок JavaScript и переполнения нет.
  Временный случайный пользователь удалён, исходный auth-файл восстановлен точно.
  Без авторизации оба сайта отвечают 401. Nginx/Xray активны, хеши конфигурации прежние.
- На HK не запускались тесты, сборки, backfill или приёмочные анализы.
  Платных OpenAI-генераций при разработке/приёмке: 0.

Артефакты: `/home/botkin/artifacts/stage9.2/` — `check-local-v2.log`,
`registry/image.env`, `registry-acceptance.log`, `acceptance-result.json`,
`no-ai/daily.{html,json,txt}`, `hk-deploy.log`, `hk-smoke.log`,
`hk-metadata-{before,after}.json`, `hk-browser-result.json`, `hk-final.log`,
скриншоты `ui/hk-*.png` и `ui/app-*.png`.

## Уточнение шапки по замечанию владельца

AI-подпись перенесена из строки даты под часовой пояс: приглушённый цвет,
13 px, обычное начертание. Подробности остаются свёрнутыми и раскрываются нажатием.

- Код `a1fcde7ebda7380e9c6d9d06c7063f882697a7b0`, тег
  `release-9.2-20260908-subtle-ai-header`;
  [CI](https://github.com/tupoybot/ZontAnalyzer/actions/runs/34215849031) прошёл.
- Образ `ghcr.io/tupoybot/zontanalyzer@sha256:d3a15a262a59b61dba40efcdaad0e9bba16647becfc73ca85d20a048e46c8a15`;
  checkout `/opt/zont-analyzer/releases/20260908-stage9.2-ai-header-a1fcde7`.
- Локально: 593 теста, Ruff, mypy, wheel/sdist, сборка и Compose config прошли.
  Опубликованный образ проверен на локальной копии: 146 отчётов, 144 публикации,
  без новых анализов и изменения пользовательских данных, 25,01 с.
- Перед deployment создан проверенный online backup
  `/opt/zont-analyzer/data/backups/zont-analyzer-20260908T103902459989Z.sqlite3`.
  Миграция не требовалась. Worker завершил цикл `ok` в 10:42:08 UTC,
  контейнер healthy, перезапусков 0; nginx/Xray активны.
- Desktop/mobile 1358/390 на живом сайте: строка ниже часового пояса,
  шрифт 13 px/400, раскрытие работает; ошибок JavaScript и переполнения нет.
  Пользовательские данные и имевшиеся перед обновлением 32 записи AI-журнала
  сохранены. Генерации AI для этой правки не выполнялись.
- Временный доступ удалён; после краткого отказа SSH очистка успешно повторена,
  исходный хеш auth-файла подтверждён. Артефакты и скриншоты:
  `/home/botkin/artifacts/stage9.2/header/`.
