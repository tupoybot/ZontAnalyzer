# Текущий статус

Обновлено: 2026-09-07.

- Этапы до **8.5 включительно приняты владельцем и слиты в `main`**;
  merge этапа 8.5 — `6c78836`. Повторная приёмка этих этапов не требуется.
- Текущий план: **9 — опытно-промышленная эксплуатация и доработки**.
  Стоимость газа (9.1) и сопровождение моделей AI (9.2) запланированы, код ещё не реализован.
- Этапы **10 и 11 необязательны и отложены**. Реализация не начата.
- Приложение по-прежнему только читает ZONT.

## Развёрнутая версия и отличие main

HK: `20260907-stage8.5-polish-cd4ae41`, тег `release-8.5-20260907-polish`;
образ `ghcr.io/tupoybot/zontanalyzer@sha256:6a2794638ce6cfd603214ff82f3b58fbb55ec8de0393472aadeec74e9ec418a2`.
Последняя принятая правка `dbbbf4e` убирает слово «оценено» под расходом газа;
она включена в `main` **без деплоя по указанию владельца**.
Документационный рефакторинг также не требует деплоя.

Доказательства последнего функционального выпуска: 433 теста, Ruff/mypy,
wheel/sdist в Docker, локальная проверка 143 отчётов на изолированной копии,
Chromium desktop/mobile для четырёх типов отчётов и короткий HK smoke прошли.
Пользовательские данные и AI-поля сохранены. Это результаты выпуска 8.5,
а не новые проверки в ходе редактирования документации.

## Текущая передача

Документация разделена на актуальные требования и явный архив. В плане 9.1
зафиксированы исторические тарифы, выбор валюты и денежные сравнения; сложная
метрика «благодаря приложению» отложена. Ограничение ожидания публикации комментария
остаётся в [backlog](./publication-backlog.md); необходимость очереди пока не доказана.

Архивная архитектура перечитана полностью. Сохранены действующие основания и
инварианты: достоверность, семантика подсистем, хранение, публикация, оценка качества.
Старые предполагаемые команды/таблицы/пороги не объявляются текущим контрактом;
вопросы хранения и внешней погоды вынесены в дорожную карту. 9.2 оформлен отдельно:
пересмотр моделей раз в 60 дней, настройки и достоверная подпись модели в отчёте.

Документационная ветка: `docs/operations-roadmap`. Проверки локальных Markdown-ссылок,
якорей и `git diff --check` прошли;
тесты приложения, сборка образа, OpenAI и deployment не нужны. Приёмка этой
ветки владельцем ещё не получена; merge не выполняется.
Следующий шаг после приёмки документации — выбрать доработку из
[плана](./implementation_plan.md), ближайшая предложенная — 9.1.
