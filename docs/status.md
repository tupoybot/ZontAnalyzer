# Текущий статус

Обновлено: 2026-09-13.

- Этапы до **8.5**, доработки **9.1/9.2**, актуальность AI, тепловые наблюдения,
  сохранение/дата газа и [антилегионелла](./antilegionella-detection.md) приняты
  владельцем и слиты в `main`.
- [Задержка дневного отчёта до 01:00 и расшифровка PID/ПЗА](./zont-control-settings.md)
  развёрнуты для просмотра. Владелец выбрал задержку вместо расширения сравнения
  `facts-v2`; отображение настроек ещё не принято.
- По просьбе владельца добавлена [инкрементальная публикация](./incremental-publication.md).
  Штатный цикл пересчитывает только изменённые отчёты, не более восьми за вызов.
  Healthcheck и его настройки не менялись.
- Известный дефект среднего превышения температуры остаётся открытым.
  Этапы **10/11** необязательны и отложены. Приложение только читает ZONT.

## Развёрнутая версия

HK: `20260913-incremental-6ccae88`, commit `6ccae88`,
тег `release-9-incremental-publication-20260913-3`;
образ `ghcr.io/tupoybot/zontanalyzer@sha256:3de7e267c24ba17f3e90d9a934f49fb4ebcf9aafe787d1b2016e6e56ef1dfd36`.
Миграция `f1a2b3c4d5e6`. Сайт: [za.tupoybot.ru](https://za.tupoybot.ru/).
Первый штатный цикл завершён `ok` **13:59:54 UTC**: 149 записей архива,
8 обработано, 141 ожидает следующих циклов; API health 200, Basic Auth 401
без авторизации. Сохранённый AI и блок настроек проверены. Nginx/Xray активны.
Docker healthcheck в момент smoke ещё `starting`; его прежние тайм-ауты
не входят в эту доработку. [Предыдущий выпуск](./zont-control-settings.md).

## Активные контракты

- [Газ и стоимость](./gas-cost-plan.md), [сопровождение моделей AI](./model-maintenance-plan.md)
  и [оценочные пакеты](./model-evaluation.md) остаются действующими.
- Пересчёт сохраняет AI. Служебные метаданные и собственная версия отчёта не
  создают ложное изменение фактов; старое несовпадение hash не доказывает изменение.
- [Дата показания газа](./gas-reading-date.md) независима от отчёта; перенос имеет
  аудит и не перезаписывает чужую запись. [Газовый PUT](./gas-save-background.md)
  подтверждает запись без ожидания публикации, HTML обновляет штатный worker.
- Worker ждёт 60 минут после местной полуночи перед первым дневным отчётом;
  синхронизация продолжается, повторный AI не выполняется. Новые поля PID/ПЗА
  применяются в следующем анализе; исторические AI-ответы не подменяются.
- Настроенное регулирование, текущая работа и история за период различаются.
  В проверенном снимке: по воздуху с ПИД, P=10/I=1, ПЗА ограничивает расчётную
  температуру; семь точек совпали с интерфейсом производителя.
- Журнал и очередь публикации переживают перезапуск. Поздняя телеметрия,
  калибровка газа, тарифы и зависимые сравнения сохраняют корректное обновление.
  Последний дневной отчёт имеет приоритет; массовые изменения обновляются пакетами.

## Проверки и следующий шаг

- **667 тестов**, Ruff/mypy, wheel/sdist, браузер, Basic Auth и Compose прошли локально;
  [CI финального кандидата](https://github.com/tupoybot/ZontAnalyzer/actions/runs/34760856245) зелёный.
- На свежей online-копии все **149 JSON** совпали с полным пересчётом, AI и исходные
  поля **151** версии сохранены. При 1 CPU/384 МБ полный проход 7,9–10,6 с,
  пустая очередь 11–12 мс; при 1095 синтетических отчётах — те же 11–12 мс.
- Опубликованный immutable image проверен отдельно. Backup, upgrade/downgrade,
  сравнение и измерения выполнены локально; на HK только миграция, деплой и smoke.
  [Подробные доказательства и ограничения](./incremental-publication.md).
- Следующий шаг: просмотр владельцем блока настроек и принятие доработки публикации.
  Ветка `stage9/report-delay-control-settings` открыта; merge требует явной приёмки.

## Лёгкий healthcheck — 2026-09-13

- Ветка `stage9/lightweight-healthcheck` от принятой оптимизации публикации
  (`main`, merge `0c266bf`). Cloud-first рабочая ветка сохранена отдельно.
- Docker проверяет только JSON heartbeat стандартной библиотекой Python.
  Импорт приложения, открытие БД и обслуживание рекомендаций исключены.
  Семантика состояний и порог 15 минут сохранены; timeout снижен до 5 секунд.
  Нестандартный путь/порог задаются через Compose environment interpolation:
  [инструкция](../deploy/OPERATIONS.md#лёгкий-docker-healthcheck).
- Локальные проверки и выпуск выполняются; merge после приёмки владельца.
