# Текущий статус

Обновлено: 2026-09-24.

- Активен отдельный [технический этап оптимизации CI](./ci-optimization.md),
  [issue #32](https://github.com/tupoybot/ZontAnalyzer/issues/32): исходные
  сопоставимые замеры локально и в Actions. М3/М4 закрыты; М5 не начат.

- [Значимые события и дневной расход газа](./report-highlights-gas.md) развёрнуты:
  отбор и группировка событий, столбчатые диаграммы в недельных/месячных отчётах.
  [PR #9](https://github.com/tupoybot/ZontAnalyzer/pull/9) принят владельцем 2026-09-21.
- [Исправления отчётов, аптайма, рекомендаций моделей и синхронизации](./report-sync-reliability.md)
  развёрнуты; [PR #7](https://github.com/tupoybot/ZontAnalyzer/pull/7) принят
  и слит владельцем в `main` (`de16b8a`).
- [Уточнение доступности и формулировок](https://github.com/tupoybot/ZontAnalyzer/pull/8)
  принято и слито в `main`: допуск 30 минут, вся телеметрия устройства вместо одного
  напряжения, отдельный статус потери связи и догрузка после восстановления.
- Этапы до **8.5**, доработки **9.1/9.2**, актуальность AI, тепловые наблюдения,
  сохранение/дата газа и [антилегионелла](./antilegionella-detection.md) приняты
  владельцем и слиты в `main`.
- [Задержка дневного отчёта до 01:00 и расшифровка PID/ПЗА](./zont-control-settings.md)
  развёрнуты для просмотра. Владелец выбрал задержку вместо расширения сравнения
  `facts-v2`; отображение настроек ещё не принято.
- По просьбе владельца добавлена [инкрементальная публикация](./incremental-publication.md).
  Штатный цикл пересчитывает только изменённые отчёты, не более восьми за вызов.
  Принято и слито в `main` (`0c266bf`).
- Известный дефект среднего превышения температуры остаётся открытым.
  Этапы **10/11** необязательны и отложены. Приложение только читает ZONT.

## Развёрнутая версия

Commit `4707a9e`, тег `release-9-report-highlights-gas-20260921-r2`;
образ `ghcr.io/tupoybot/zontanalyzer@sha256:58b0d4d36f853bfe2ce0a851330510e830c3994b5342cf86447c7c1782c10f71`.
Миграция `f1a2b3c4d5e6`, схема БД не менялась.
Worker healthy. Установлены 157 обновлённых отчётов; manifest сохраняет 159 записей.
Перекрытие запросов — 120 минут, период опроса — 30 минут.
Docker healthcheck использует curl и HTTP heartbeat, без запуска Python.

## Активные контракты

- [Газ и стоимость](./gas-cost-plan.md), [сопровождение моделей AI](./model-maintenance-plan.md)
  и [оценочные пакеты](./model-evaluation.md) остаются действующими.
- Пересчёт сохраняет AI. Служебные метаданные и собственная версия отчёта не
  создают ложное изменение фактов; старое несовпадение hash не доказывает изменение.
- [Дата показания газа](./gas-reading-date.md) независима от отчёта; перенос имеет
  аудит и не перезаписывает чужую запись. [Газовый PUT](./gas-save-background.md)
  подтверждает запись без ожидания публикации, HTML обновляет штатный worker.
- Worker ждёт 60 минут после местной полуночи перед первым дневным отчётом;
  синхронизация продолжается, повторный AI не выполняется. Новые поля PID/ПЗА
  применяются в следующем анализе; исторические AI-ответы не подменяются.
- Настроенное регулирование, текущая работа и история за период различаются.
- Журнал и очередь публикации переживают перезапуск. Поздняя телеметрия,
  калибровка газа, тарифы и зависимые сравнения сохраняют корректное обновление.
  Последний дневной и новые неопубликованные отчёты имеют приоритет;
  массовые изменения обновляются пакетами.

## Проверки и следующий шаг

- **723 теста**, Ruff/mypy и wheel/sdist прошли локально в Docker; браузер,
  Basic Auth и Compose проверены. [CI релиза](https://github.com/tupoybot/ZontAnalyzer/actions/runs/35627761106) зелёный.
- Опубликованный immutable image проверен на отдельной записываемой онлайн-копии БД:
  integrity/FK, сохранность пользовательских таблиц, событий и AI-ответов.
  Недельная/месячная диаграммы проверены на экранах 1440/390 px; пропуски сохранены.
- Архив и дневные значения газа подготовлены локально. Установка с проверкой исходных
  версий отчётов и пользовательских данных прошла; повторный импорт локально идемпотентен.
  На сервере выполнены установка артефактов, деплой и короткий smoke:
  healthy, HTTP heartbeat 200, отчёты/архив корректны, внешний Basic Auth 401.
  Анализ архива и новые AI-вызовы при установке не запускались.
- Следующий шаг — наблюдение за отчётами в эксплуатации.
  Cloud-first продолжает обновляться от принятого `main`.

## Cloud-first

- M0 принят; действуют [контракты миграции](./cloud_first_m0.md) и
  [план M0–M8](./cloud_first_milestone.md). Новая версия предназначена для облака
  и YDB; старый production остаётся на проверенном SQLite-релизе до М8.
- [M1](./cloud_first_m1.md) принят владельцем 2026-09-22. Облачный каркас,
  защищённое состояние Terraform, Xray, HTTPS с авторизацией, Grafana и CI
  прошли технические проверки. PR #5 слит в `main` (`bb97f21`) с сохранением
  последних принятых исправлений приложения. Старый production не обновляется.
- Работающий старый выпуск закреплён веткой `old-stable` и существующим тегом.
  Правила защиты ветки в GitHub не требуются для приёмки; тег выпуска не перемещается.
- [М2](./cloud_first_m2.md) принят владельцем 2026-09-23 и слит через
  [PR #12](https://github.com/tupoybot/ZontAnalyzer/pull/12) (`2c4a60b`);
  [sub-issue #11](https://github.com/tupoybot/ZontAnalyzer/issues/11) закрыта:
  ограниченные HTTP-задания, готовность и диагностика,
  проверка внешних API, сборка и deployment облачного кандидата по digest.
  Кандидат `37a3c7c` проверен локально и развёрнут в dev; готовность, повторные
  задания, журналы, метрики, ZONT и один metadata-запрос OpenAI проверены.
  Локально прошли 759 тестов; CI кандидата и
  [deployment из CI](https://github.com/tupoybot/ZontAnalyzer/actions/runs/35897583738)
  зелёные. Повторное развёртывание не меняет ресурсы и проходит smoke.
  `cloud-dev` использует `No restriction`. Два лишних образа, тестовая и прежняя
  рабочая версии секрета удалены с подтверждением через API; текущая версия
  используется обоими контейнерами и CI. Технических блокеров М2 нет.
- [M3](./cloud_first_m3.md) принят 2026-09-24 в рамках делегированной приёмки:
  [sub-issue #13](https://github.com/tupoybot/ZontAnalyzer/issues/13).
  Кандидат `1a7de86`: 761 общий тест и 29 тестов YDB, Ruff/mypy и сборки прошли.
  Проверены недельная обезличенная выборка и smoke образа на managed YDB.
  [PR #27](https://github.com/tupoybot/ZontAnalyzer/pull/27),
  [CI](https://github.com/tupoybot/ZontAnalyzer/actions/runs/35922404864) прошёл.
  Проверочные пространства удалены с подтверждением API; ресурсы и лимиты не изменены.
- [М4: полный цикл отчёта](./cloud_first_m4.md) принят владельцем 2026-09-24
  и слит через [PR #28](https://github.com/tupoybot/ZontAnalyzer/pull/28) (`5b3f75c`).
  Прикладные операции, локальные тесты и CI используют YDB; SQLite/Alembic
  удалены из нового runtime. Полный импорт и повтор, сравнение фактов
  суточных, недельных и месячных отчётов, публикация, обратная связь
  и продолжение заданий проверены.
  Локально прошли 849 тестов, Ruff/mypy, установленный CLI и wheel/sdist.
  Кандидат `23f2d24` прошёл CI, браузерную проверку и smoke опубликованного образа;
  [сценарий развёртывания](https://github.com/tupoybot/ZontAnalyzer/actions/runs/35982790171)
  автоматически выбрал его без изменения секрета окружения. Облачный цикл прошёл
  `pending → done → done` с повторным использованием отчёта и без AI.
  Проверочные данные и заменённый образ удалены с подтверждением API;
  повторный план не меняет ресурсы. Итоговая сверка ресурсов прошла;
  [задача #15](https://github.com/tupoybot/ZontAnalyzer/issues/15) закрыта.
  Старый production не изменён. До М5 выполняется отдельный технический этап CI.
- Каждый облачный этап заканчивается проверенной через API очисткой временных
  и заменённых ресурсов. Сохранённые платные ресурсы должны использоваться
  действующим потребителем или иметь подтверждённое назначение для восстановления.
