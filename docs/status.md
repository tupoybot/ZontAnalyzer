# Текущий статус

Обновлено: 2026-09-13.

- Этапы до **8.5**, доработки **9.1/9.2**, актуальность AI, тепловые наблюдения,
  сохранение/дата газа и [антилегионелла](./antilegionella-detection.md) приняты
  владельцем и слиты в `main`.
- Согласована [задержка дневного отчёта до 01:00 и расшифровка PID/ПЗА](./zont-control-settings.md).
  Кандидат в `stage9/report-delay-control-settings`; CI и локальная приёмка пройдены.
  Выпуск HK остановлен из-за тайм-аутов healthcheck; возвращён прежний образ,
  worker снова `ok`, но Docker healthcheck всё ещё превышает тайм-аут.
  Владелец выбрал задержку вместо расширения сравнения `facts-v2`.
- Известный дефект среднего превышения температуры остаётся открытым.
- Этапы **10/11** необязательны и отложены. Приложение только читает ZONT.

## Развёрнутая версия

HK: `20260911-antilegionella-7383ff4`, commit `7383ff4`,
тег `release-9-antilegionella-20260911`;
образ `ghcr.io/tupoybot/zontanalyzer@sha256:52ba083de72d6fe321e12fa16deffcce065d1a0606a3f16f8b6fe3236255b3b1`.
Миграция `d92a10b4c601`. Сайт: [za.tupoybot.ru](https://za.tupoybot.ru/).
Последняя проверка HK: прежний image возвращён, HTTP health 200, worker `ok`
в 05:38:15 UTC, штатный latest обновлён. Docker healthcheck остаётся `unhealthy`
из-за тайм-аутов запуска проверки.
[История проверок предыдущих выпусков](./archive/status-2026-09-11.md).

## Активные контракты

- [Газ и стоимость](./gas-cost-plan.md), [сопровождение моделей AI](./model-maintenance-plan.md)
  и [оценочные пакеты](./model-evaluation.md) остаются действующими.
- Пересчёт сохраняет AI. Служебные метаданные и собственная версия отчёта не
  создают ложное изменение фактов; старое несовпадение hash не доказывает изменение.
- [Дата показания газа](./gas-reading-date.md) независима от отчёта; перенос имеет
  аудит и не перезаписывает чужую запись. [Газовый PUT](./gas-save-background.md)
  подтверждает запись без ожидания публикации, HTML обновляет штатный worker.
- Кандидат ждёт 60 минут после местной полуночи перед первым дневным отчётом;
  синхронизация продолжается, повторный AI не выполняется. Новые поля PID/ПЗА
  применяются в следующем анализе; исторические AI-ответы не подменяются.
- Настроенное регулирование, текущая работа и история за период различаются.
  В проверенном снимке: по воздуху с ПИД, P=10/I=1, ПЗА ограничивает расчётную
  температуру; семь точек совпали с интерфейсом производителя.

## Проверки и следующий шаг

- Кандидат: финальный Docker-набор **646 тестов**, Ruff/mypy и wheel/sdist прошли.
  Браузер, Basic Auth и Compose прошли. [Подробные доказательства](./zont-control-settings.md).
- На изолированной online-копии метрики, события, качество, сохранённые отчёты
  и старый AI неизменны; расшифрованные настройки целиком входят в пакет.
- Один локальный OpenAI-вызов успешен: `analyst-v8.4` правильно описал PID/ПЗА.
  Время снимка привязано к источнику и проверено на сохранённом ответе без
  второго запроса. Точность остальных временных формулировок AI требует наблюдения.
- CI и проверка опубликованного immutable image прошли. Полная локальная публикация
  149 отчётов при 1 CPU/384 МБ завершилась за 21 секунду. На HK новый контейнер
  отвечал HTTP, но не прошёл healthcheck за отведённое время. Публикация завершилась
  только перед остановкой. Прежний образ тоже превысил стартовый healthcheck;
  короткое измерение HK показало CPU steal 57,6%.
- Следующий шаг: выяснить причину тайм-аутов Docker healthcheck на HK перед
  повторным выпуском. Merge не разрешён заранее.
