# Текущий статус

Обновлено: 2026-09-08.

- Этапы до **8.5 включительно приняты владельцем и слиты в `main`**.
- **9.1 — стоимость газа:** реализован и развёрнут из `stage9/gas-cost`, ожидает принятия владельцем. Merge только после явного принятия владельцем.
- **9.2** запланирован; **10 и 11** необязательны и отложены.
- Приложение только читает ZONT.

## Развёрнутая версия

HK: `20260907-stage9.1-wrap-fe31574`, тег `release-9.1-20260907-wrap`;
образ `ghcr.io/tupoybot/zontanalyzer@sha256:35eec64d2e0d6a9b50dd7a17e81793249bf0525fbd9655b78300e38ed50a430e`.
Контейнер healthy; короткий read-only smoke API, авторизации и desktop/mobile прошёл.
Пользовательские данные, число отчётов и учёт AI не изменились. Резервная копия:
`/data/backups/zont-analyzer-20260907T192120775925Z.sqlite3`.

## Контракт и текущая передача 9.1

- Цена/валюта меняются рядом с показанием газа. Обычное изменение — со следующего
  календарного месяца; последнее сохранение планового месяца действует, прежние
  значения остаются в аудите. Исторические месяцы можно внести явно.
- Decimal-стоимость рядом с общим объёмом, без подписи «оценка».
  Легенда распределения на дашборде — без денег; подробные метрики содержат суммы. Неизвестные/частичные суммы обозначены,
  валюты не конвертируются. Выводы об эффективности не зависят от смены цены.
- Числовые поля принимают точку и запятую, ошибочный ввод не очищает данные.
  Проверка показания видит текущий паспортный максимум; дата применимости
  по-прежнему ограничивает сравнение прошлых показаний.
- На изолированной online-копии: миграция и публикация 143 отчётов сохранили
  показания, пользовательские данные, AI-тексты, объёмы газа и надёжность.
  Месячные отчёты исходно не имеют полного газового объёма — сумма неизвестна.
- Docker: **511 тестов**, Ruff, mypy, wheel/sdist и сборка production-образа прошли.
  HTML/JSON/text проверены для четырёх типов отчётов; desktop/mobile 1280/390 px,
  браузерное сохранение тарифов и валидация чисел проверены.
  Артефакты: `/home/botkin/artifacts/stage9.1/`.
  Registry-образ проверен локально; CI ветки и релизного тега прошли.
  OpenAI-запросов в работе над 9.1 не выполнялось.

Подробный контракт: [стоимость газа](./gas-cost-plan.md).

Дополнение 9.1 развёрнуто:
заголовок недневных отчётов содержит обе даты, дневного — одну; неизвестная
стоимость не добавляет подпись и разделитель рядом с объёмом. В подробных метриках
газа известные суммы добавлены также к расходу по модели и его составляющим;
легенда распределения на дашборде остаётся без денег.
На дашборде объём и стоимость — в одной строке через `·`; при нехватке места сумма
с единицами переносится целиком. Надёжность в скобках после «Расход газа». Проверены 15 комбинаций значений до 9999,99 м³ /
99 999,99 руб. и ширины экрана 320–1280 px; переполнений и разрыва чисел нет.
Релизный CI `34154802245` прошёл; registry-образ проверен локально на 143 отчётах.
Рабочая страница обновилась штатной публикацией; smoke 1280/390 px прошёл.
Артефакты дополнения: `/home/botkin/artifacts/stage9.1-wrap/`.
Следующий шаг — явная команда владельца на merge; слияние не выполнялось.

Корневой README обновлён: текущие возможности, месячные тарифы, веб-формы,
команды публикации и статус каналов доставки; локальные ссылки проверены.

Готовится исправление пояснений: автоматический пересчёт после синхронизации
показывает причину сохранения AI-ответа и его исходное время. Повторные пересчёты
не сдвигают это время. В интерфейсе — названия часовых поясов ZONT; технические
имена полей заменяются в пользовательской прозе, исходный AI/JSON сохраняется.
