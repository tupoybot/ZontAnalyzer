# Текущий статус

Обновлено: 2026-09-10.

- Этапы до **8.5**, доработки **9.1**, **9.2**, актуальность AI, сноска о диапазоне
  уставки и полезные тепловые наблюдения приняты владельцем и слиты в `main`.
- [Сохранение газа без ожидания HTML](./gas-save-background.md): развёрнуто в
  `stage9/gas-save-background`, ожидает приёмки владельца; в `main` не слито.
- В той же ветке реализуется [выбор даты показания](./gas-reading-date.md),
  включая разрешённый перенос 1929 м³ с 9 на 10 сентября. Время не добавляется.
- Известный дефект среднего превышения температуры остаётся открытым.
- Этапы **10 и 11** необязательны и отложены. Приложение только читает ZONT.

## Развёрнутая версия

HK: `20260910-gas-save-background-4545ee2`, commit `4545ee2`,
тег `release-9-gas-save-background-20260910`;
образ `ghcr.io/tupoybot/zontanalyzer@sha256:c68b355b267b2ad29d9a0ced7cf5fa880a7f3f437dd062a862b2246c5c9c6530`.
Миграция `d92a10b4c601`, изменений схемы нет.
Сайт: [za.tupoybot.ru](https://za.tupoybot.ru/).
[Предыдущий выпуск и его проверки](./archive/thermal-observations-release-status.md).

## Активные контракты

- [9.1 — стоимость газа](./gas-cost-plan.md): тарифы, валюты и сравнения.
- [9.2 — сопровождение моделей](./model-maintenance-plan.md): версионируемые
  настройки AI, аудит и происхождение ответа; настройки применяются к новым
  анализам. Пересмотр каталога раз в 60 дней, автоматической смены модели нет.
- [Оценочные пакеты и CLI](./model-evaluation.md): восемь обезличенных сценариев;
  сравнительное качество кандидата требует сохранённой локальной оценки.
- Повторный расчёт сохраняет AI; служебные метаданные и собственная сохранённая
  версия отчёта не должны создавать ложное изменение фактов. Старое несовпадение
  контрольных сумм не доказывает изменение показателей.
- Газовый PUT подтверждает запись без ожидания публикации. Форма получает
  показание сразу; HTML и газовую аналитику обновляет ближайший успешный цикл
  worker. Ошибка цикла задерживает HTML, но не отменяет сохранённое показание.

## Проверки и следующий шаг

- Docker: **601 тест**, Ruff, mypy, wheel/sdist; браузер, Basic Auth и Compose — прошли.
  [CI выпуска](https://github.com/tupoybot/ZontAnalyzer/actions/runs/34451649962) зелёный.
- Immutable-образ проверен локально на онлайн-копии БД: PUT при занятой публикации
  ответил за **0,023 с**; показание 1929 м³, история и AI-журнал сохранены. После
  перезапуска runtime опубликованы 146 отчётов. Ошибки, повторы, исправления и
  удаления покрыты регрессионными тестами. OpenAI при проверках не вызывался.
- HK: контейнер healthy, HTTP health `ok`, worker `ok`, `analyzed_dates: []`; новый интерфейс появился
  в штатном `latest.html`. 1929 м³ за 9 сентября и единственная запись аудита на месте.
  Nginx/Xray активны; внешний запрос без авторизации получает 401.
- Смысловая проверка промпта `analyst-v8.3` остаётся открытой: предыдущая локальная
  попытка OpenAI получила HTTP 403. Промпт используется в следующих штатных анализах;
  в этой доработке AI не менялся.
- Следующий шаг: приёмка владельцем сохранения газа, затем merge этой ветки.
