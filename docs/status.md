# Текущий статус

Обновлено: 2026-09-08.

- Этапы до **8.5** и доработка **9.1 приняты владельцем и слиты в `main`**.
  Приёмка 9.1 получена 2026-09-08; merge `f3aca57`. Перед merge локально выполнен
  `docker system prune -af`; на HK очистка не выполнялась.
- **9.2 — модели AI:** реализация и проверки в `stage9/ai-models`.
- Этапы **10 и 11** необязательны и отложены. Приложение только читает ZONT.

## Развёрнутая версия

HK: `20260908-stage9.1-period-refresh-93cb7a6`, тег `release-9.1-20260908-period-refresh-v2`;
образ `ghcr.io/tupoybot/zontanalyzer@sha256:613c6eb937810263a8d81c8918af5c996db932297b9017b42e4be9222da93921`.
История проверенного выпуска: [9.1](./archive/stage9.1-release-status.md).
Активные контракты стоимости газа: [план 9.1](./gas-cost-plan.md).

## Текущая работа 9.2

- Единые настройки AI с веб-переопределением, аудитом и защитой от устаревшего
  сохранения. Версия параметров фиксируется на весь анализ.
- Происхождение успешного AI-ответа сохраняется с ним; кэш и неудачная регенерация
  не переименовывают прежний ответ. Историческая модель не угадывается.
- Пересмотр официального каталога и снятия с поддержки отдельно от анализа;
  интервал 60 дней, ручная проверка, решения владельца и ограниченные повторы.
- Модель и параметры не входят в признак необходимости календарного пересчёта.
  Переход старых признаков проверяется без повторного AI.
- Требования и критерии: [план 9.2](./model-maintenance-plan.md).

## Проверки и следующий шаг

- Проверки настроек, HTTP оборудования и миграции расписания: 20 прошли.
- Проверки пересмотра, источников, повторов и решений: 10 прошли.
- Docker: 592 теста, Ruff, mypy, wheel/sdist прошли.
- На online-копии: 146 отчётов сохранены, 144 опубликованы; цикл worker без
  пересчётов. Пользовательские данные и учёт AI неизменны. Отдельный no-AI
  дневной отчёт содержит 41 метрику и 15 событий.
- Браузер 1280/390: реальные локальные API, сохранение/сброс AI, источники,
  подпись модели; ошибок JavaScript и переполнения нет.
- Живые официальные страницы проверены локально через HTTP-прокси; HK HTTP 200.
- Реальных запросов генерации OpenAI в этой итерации пока не выполнялось.
- Артефакты: `/home/botkin/artifacts/stage9.2/`.
- Следующий шаг: завершить проверки, собрать immutable-образ и проверить его
  на локальной online-копии перед deployment и коротким HK smoke. Merge 9.2
  только после отдельной явной приёмки владельцем.
