# Текущий статус

Обновлено: 2026-09-08.

- Этапы до **8.5 включительно приняты владельцем и слиты в `main`**.
- **9.1 — стоимость газа:** реализован и развёрнут из `stage9/gas-cost`, ожидает принятия владельцем. Merge только после явной команды владельца.
- **9.2** запланирован; **10 и 11** необязательны и отложены.
- Приложение только читает ZONT.

## Развёрнутая версия

HK: `20260908-stage9.1-period-refresh-93cb7a6`, тег `release-9.1-20260908-period-refresh-v2`;
образ `ghcr.io/tupoybot/zontanalyzer@sha256:613c6eb937810263a8d81c8918af5c996db932297b9017b42e4be9222da93921`.
Контейнер healthy; короткий read-only smoke API, авторизации и desktop/mobile прошёл.
Пользовательские данные, число отчётов и учёт AI не изменились. Соседние сервисы active.
Резервная копия: `/data/backups/zont-analyzer-20260908T071226782350Z.sqlite3`.
Повторный деплой переиспользовал эту проверенную копию: схема не менялась,
состояние владельца и учёт AI повторно сверены с моментом создания копии.

## Активные контракты 9.1

- Цена/валюта меняются рядом с показанием газа. Обычное изменение — со следующего
  календарного месяца; последнее сохранение планового месяца действует, прежние
  значения остаются в аудите. Исторические месяцы можно внести явно.
- Стоимость рядом с общим объёмом, без подписи «оценка». При нехватке места число
  с единицами переносится целиком; надёжность в скобках после «Расход газа».
  Легенда распределения на дашборде — без денег; подробные метрики содержат известные
  суммы. Неизвестная стоимость не добавляет подпись и разделитель рядом с объёмом.
  Валюты не конвертируются; выводы об эффективности не зависят от смены цены.
- Числовые поля принимают точку и запятую, ошибочный ввод не очищает данные.
  Проверка показания видит текущий паспортный максимум; дата применимости
  по-прежнему ограничивает сравнение прошлых показаний.
- Заголовок недневных отчётов содержит обе даты, дневного — одну.
- Изменения телеметрии вне точных границ периода не запускают повторный расчёт.
  Маркеры суток UTC используются только для кэша. Переход старых ревизий и подписей
  календарного планировщика выполняется без повторного анализа и AI.
- Успешный AI-ответ сохраняет отпечаток фактов. Пересчёт с теми же фактами не делает
  его устаревшим; исходные отпечаток и время сохраняются при повторных пересчётах.
  У старых уже перезаписанных ответов без отпечатка сравнение недоступно — интерфейс
  сообщает это, не утверждая изменение фактов. Неудачное обновление AI и обновление
  только расчёта газа объясняются отдельно.
- Часовые пояса отображаются по словарю ZONT (например, `UTC+4 — Самара, Удмуртия`);
  идентификаторы для вычислений сохраняются. В пользовательской прозе технические
  имена заменены понятными описаниями; исходные AI-ответы, JSON и ссылки на свидетельства сохраняются.
- Корневой README актуализирован: возможности, месячные тарифы, веб-формы,
  команды публикации и статус каналов доставки.

Подробный контракт: [стоимость газа](./gas-cost-plan.md).

## Проверки и следующий шаг

- Релизный CI `34199205823`: **547 тестов**, Ruff, mypy, сборка пакета и образа прошли.
- Опубликованный immutable-образ проверен локально на изолированной online-копии:
  публикация 144 отчётов и полный цикл дневного/календарного планировщиков без новых
  расчётов. Исходники совпадают с кандидатом, пользовательские данные и AI сохранены.
  Запросов к OpenAI для проверки не делали.
- Исправлена причина ложного пересчёта ночного отчёта: общие сутки UTC захватывали
  сегодняшние показания за пределами вчерашнего локального дня. Старая пометка сама
  по себе не доказывает изменение фактов. Исходный снимок AI этого отчёта утрачен.
- Первая попытка выпуска остановлена из-за совместимости подписей календарного
  планировщика; предыдущий образ и служебные ревизии восстановлены, AI-запросов
  не добавилось. Исправление дополнено регрессией и проверкой полного планировщика.
- Кэш 276 точных ревизий подготовлен в локальном Docker и перенесён на HK только
  после совпадения идентификатора базы и маркеров соответствующих исходных данных.
  Повторная проверка 128 дневных ревизий на копии занимает около 0,14 секунды.
- Браузерная регрессия форм и переноса значений 320–1280 px прошла.
  После штатной публикации на HK smoke 1280/390 px подтвердил новые подписи,
  целые числа при переносе и отсутствие технических имён в видимом тексте.
  Ночной отчёт сохранил `generated_at` и AI; ревизия обновлена до v2. Завершённый
  рабочий цикл: `state=ok`, `analyzed_dates=[]`, `long_periods=[]`; после публикации
  состояние владельца и 31 запись учёта AI остались неизменны.
  Временный доступ для smoke удалён, исходная конфигурация авторизации восстановлена.
- Артефакты: `/home/botkin/artifacts/stage9.1-period-refresh/`;
  базовая приёмка тарифов: `/home/botkin/artifacts/stage9.1/`.
- Следующий шаг — явная команда владельца на merge. До merge выполнить локально
  `docker system prune -af`; на HK эту очистку не запускать. Слияние не выполнялось.
