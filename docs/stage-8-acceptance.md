# Этап 8 — расход газа и экономия

Статус: реализация и техническая проверка. Ветка `stage8` не слита;
приёмка владельца ещё не запрашивалась. Матрица: [stage-8-evaluation.md](./stage-8-evaluation.md).

## Контракты

Показания и профиль оборудования этапа 3 сохранены. Новый детерминированный модуль
работает с активностью `fl`, усреднённой калибровкой и поддержанными диапазонами
модуляции. Подробности параметров, поддержки и отложенной проверки сохраняются
в версии модели. Дневные достаточные статистики кэшируются по ревизии телеметрии.

Версии моделей и исходные входы сохраняются в существующем `app_meta`; новая
миграция не требуется. Публикация пересчитывает зависимые оценки локально,
обновляет canonical context под optimistic guard и не переписывает рекомендации,
feedback, дату исходного AI-ответа или очередь уведомлений. Старый AI-текст
помечается историческим. Исходный прогноз и газовый контекст сохраняются при
фиксации ручного эксперимента.

Все виды отчётов показывают газ, индекс надёжности, диапазон, покрытие, версию,
средние с явным знаменателем и часы наблюдаемой работы горелки. Карточка газа
занимает свободную ячейку основной информации. Подробности доступны отдельно.
Вопрос для AI и кнопка перегенерации выровнены на desktop и расположены вертикально
на mobile.

Погодное сравнение использует только предшествующие независимые интервалы.
Последующие показания и модельные оценки различаются. Арифметика остаётся локальной;
`analyst-v8` объясняет свидетельства, условия прогноза и один ручной эксперимент.
Никакой записи в ZONT нет.

## Изолированная приёмка

Артефакты: `/home/botkin/artifacts/stage8/`.
Online backup HK: `/opt/zont-analyzer/data/backups/zont-analyzer-20260907T092657746445Z.sqlite3`.
Копия `source/production.sqlite3`, отдельная рабочая БД `data/zont-analyzer.sqlite3`,
публикация `publish/`. Тяжёлые проверки выполняются только локально.

В копии четыре реальных показания: 20/29/31 августа и 5 сентября; указан
максимальный расход 2.69 м³/ч, газовая плита отмечена отсутствующей. Калибровка
этого дома предварительная: нет доказанной таблицы всех режимов и точности для
малой экономии. Отсутствие достаточной истории ограничивает вывод, а не подменяется
синтетическими данными в рабочей БД.

Финальные результаты тестов, AI-проверки, commit/tag/digest и bounded smoke будут
зафиксированы здесь до запроса приёмки владельца.

## Технические проверки перед выпуском

- Полный `pytest`: 371 passed; после защиты от округления погодного покрытия
  дополнительно 9 интеграционных проверок газового цикла — passed.
- Ruff и strict mypy: passed (63 файла приложения).
- `python -m build`, сборка production/test Docker, Compose validation: passed.
- Контейнерный feedback HTTP, nginx Basic Auth и Chromium archive browser: passed.
- Визуально проверены карточка газа на desktop и вертикальная форма вопроса на
  mobile; горизонтального переполнения нет. Скриншоты в каталоге артефактов.
- Локальный контейнер без сети сформировал и экспортировал новый daily на рабочих
  данных: `offline-daily.json/html`; газ 1.078 м³, статус `estimated`.

На текущих показаниях пригодны два интервала; индекс 35, нет отложенной проверки.
Последняя неделя: оценка 47.21 м³, диапазон чувствительности 0–113.20 м³.
Месячное покрытие 55.75%: итог неизвестен, доступна только наблюдаемая часть.
Это предварительная калибровка и не основание обещать точность эффекта 2% или 5%.

## Реальный OpenAI

Использована **1 попытка из разрешённых 5**, автоматических повторов нет.
Запрос на реальном недельном отчёте из локального контейнера отклонён:
`403 unsupported_country_region_territory`. Успешных ответов — 0. Результат и
учёт вызова сохранены локально (`real-ai-1.log`, `openai-attempt-1.json`, AI ledger).
Прежний рабочий AI-ответ не заменялся. Реальная семантическая проверка `analyst-v8`
остаётся непроверенной в этом окружении; её не подменяют моковые проверки схемы,
пакета и сквозного цикла. Повтор в доступном поддерживаемом окружении должен быть
отдельно учтён в оставшемся лимите.
