# Дата снятия показания газа

Статус: развёрнуто, принято владельцем и слито в `main` 2026-09-10.
Выбор даты дополняет [быстрое сохранение](./gas-save-background.md);
обе доработки приняты вместе после подтверждения обновления расхода владельцем.

## Контракт

- В форме дневного отчёта задаются накопленное показание и календарная дата снятия.
  На выбранную дату может не существовать отчёта. Поле времени не добавляется —
  это явно исключено владельцем 10 сентября 2026.
- Для нового показания обычный отчёт подставляет свою дату; последний доступный
  завершённый дневной отчёт определяется API и подставляет текущую локальную дату
  браузера. Это работает и для `latest.html`, и для прямого адреса отчёта. При
  недоступности API и в offline HTML безопасная исходная дата — дата отчёта.
- Редактирование всегда использует сохранённую дату. «Новое показание» создаёт
  независимую запись; история позволяет выбрать старую. Изменение даты редактора
  переносит выбранную запись при сохранении, сохраняя её id и исходное время ввода.
  Аудит хранит обе даты. Конфликт с другой записью не перезаписывает её.
- Сохраняется ограничение одного показания на день. Перенос самой границы замены/
  сброса счётчика отклоняется: без отдельного редактирования границ нельзя незаметно
  изменить принадлежность остальных показаний участкам учёта. Удаление показания
  сохраняет существующую границу, как и раньше.
- Ошибки сохраняют введённые дату и значение; загрузка не перезаписывает ввод во
  время запроса. Удаление относится к выбранной сохранённой записи, даже если дата
  в редакторе изменена и ещё не сохранена.

## API и расчёт

`GET /reports/{id}/gas?day=YYYY-MM-DD` выбирает календарный день; без параметра
используется дата отчёта. Ответ содержит `report_day`, `selected_day`,
`is_latest_report`, текущую запись, список показаний установки и аудит выбранного дня.
`PUT` принимает `day`, `reading_id`, значение либо удаление. `reading_id: null`
означает создание без перезаписи; строковый id — исправление/перенос существующей
записи. Старые клиенты без этих полей сохраняют прежний контракт по дате отчёта.

Схема БД не меняется: используется существующий `reading_day`. Существующие записи
не переименовываются автоматически. Владелец отдельно разрешил исправить 1929 м³
с 9 на 10 сентября; после deployment выполнена одна обычная аудируемая коррекция.

Модель расхода уже использует `reading_day`. Проверка должна доказать, что две
записи из одного отчёта образуют интервал между выбранными датами, а перенос даты
меняет калибровочный интервал и версию модели, сохраняя сам объём разности. Точное
время не выдумывается: условный полдень и неопределённость внутри суток сохраняются.

## Проверки и выпуск

- Commit `1e9babc`, тег `release-9-gas-reading-date-20260910`.
- Образ `ghcr.io/tupoybot/zontanalyzer@sha256:bf919c8f825479c5de4d1c6acface591e5998f5701e902db06f67b68b30c406a`.
- Docker: 610 тестов, Ruff, mypy, wheel/sdist прошли; заключительная проверка
  исходников и браузер покрывают финальные изменения. Basic Auth, Compose, ссылки
  и diff проверены. [CI](https://github.com/tupoybot/ZontAnalyzer/actions/runs/34454983504)
  зелёный, включая публикацию образа.
- Онлайн-копия: `/opt/zont-analyzer/data/backups/zont-analyzer-20260910T081326476472Z.sqlite3`.
  Именно опубликованный immutable-образ проверен без сети на отдельной локальной БД
  и каталоге публикации. Перенос при удержании `.publication.lock` ответил за 0,023 с;
  id и исходное время ввода сохранены, дата изменена с 9 на 10 сентября, объём 1929.
  Опубликованы 146 отчётов; остальные ручные данные, рекомендации и AI-журнал сохранены.
  OpenAI не вызывался. Схема осталась `d92a10b4c601`.
- Артефакты: `/home/botkin/artifacts/gas-reading-date/`; `acceptance-result.json`,
  `immutable-acceptance.log`, `check-local.log`, `final-source-check.log`, `browser.log`,
  `basic-auth.log`, `gas-form.png`, `deploy.log`, `smoke.log`, `live-correction.json`.
  Локальный HTML вручную проверен: поле даты, история со значением 1929 за 10 сентября,
  обе даты в аудите переноса. Offline HTML использует дату отчёта; поведение текущей
  даты браузера проверено отдельно живым браузерным сценарием.
- HK получил только проверенный образ. Свежая проверенная копия переиспользована
  при deployment без повторного backup: `release-reuse-backup.sh` отличается от
  стандартного скрипта только этой строкой; схема не менялась.
- Контейнер healthy, HTTP health `ok`, worker `ok`, `analyzed_dates: []`.
  Штатный `latest.html` обновлён 08:36:15 UTC и содержит поле даты.
  Nginx/Xray активны, внешний запрос без авторизации получает 401.
- Разрешённая владельцем коррекция выполнена через API в 08:35:51 UTC после
  проверки исходных id/значения и отсутствия записи за целевой день. 1929 м³ теперь
  за 10 сентября; за 9 сентября записи нет. Сохранены id и исходное время ввода,
  добавлен один аудит `move`. Остальные четыре показания не изменились, всего их пять.
  Никаких синтетических записей на HK не создавалось.

Владелец явно разрешил commit/push/merge всей доработки ввода газа 2026-09-10.
