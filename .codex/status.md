# ZontAnalyzer: фактический статус

Обновлено: 2026-08-03 (Europe/Samara).

Это краткий checkpoint факта. Актуальный порядок работ находится в
[`roadmap.md`](./roadmap.md), архитектурные принципы — в [`architecture.md`](./architecture.md).

## Резюме

Рабочее read-only приложение реализовано end-to-end на одной реальной установке BAXI Connect+:
discovery → возобновляемый sync → локальный анализ отопления и ГВС → атомарный daily HTML/JSON →
опциональная OpenAI-интерпретация. Baseline до P1 сохранён отдельным Git-коммитом.

P0 и вертикальный срез P1 реализованы. Инженерная часть P2 готова к автономному пилоту: worker
догоняет пропущенные дни, обновляет `latest.html`, пишет heartbeat и не повторяет AI-вызов при
каждом poll. Формально P2 остаётся незавершённым до 2–4 недель наблюдения и обратной связи.

## P2 deployment checkpoint

- Ручной test deployment перенесён на HK `82.22.6.84` отдельным Compose project без публичных
  портов; worker image — `zont-analyzer:p2-hk-20260803-1`.
- Persistent SQLite перенесена через согласованный online backup в
  `/opt/zont-analyzer/data/zont-analyzer.sqlite3`; integrity `ok`, revision `dd4272b6d030`,
  469,820 samples и 96 отчётов. Миграционный backup сохранён в `data/backups`.
- Публичный test-канал: `https://hk.tupoybot.ru/za/`; `latest.html` и `ai-latest.html`
  возвращают HTTP 200 и содержат `AI-интерпретация: да`.
- Worker heartbeat `ok`; nginx, Xray, Postfix, OpenDKIM и Docker на HK остались active.
- После явного подтверждения владельца OpenAI включён только в test deployment; API-ключ
  смонтирован read-only из файла с mode `0600`, а обычные daily-отчёты могут запрашивать AI.
- Единственный контрольный вызов с HK получил Responses API HTTP 200, прошёл strict Structured
  Output и evidence validation; ledger вырос с 1,464 до 5,475 токенов. Автономный worker не
  повторил вызов после старта и переиспользует сохранённую AI-интерпретацию.
- Со старого `217.60.10.224` удалены только Compose project/network, два ZontAnalyzer image,
  `/opt/zont-analyzer` и прежний каталог `/za`; Nightscout остался healthy и возвращает 200.

## Проверенный факт

- Локальная БД: 40,624,128 байт, integrity `ok`, revision `dd4272b6d030`.
- 1 устройство, 39 временных рядов, 463,745 канонических samples и 13 отчётов.
- Диапазон данных: `2026-05-04T20:52:00Z` — `2026-08-02T22:44:00Z`.
- 90-дневный backfill на реальном ZONT завершался и корректно возобновлялся после rate limit.
- Неполное окно не продвигает cursor; повторная запись идемпотентна.
- Реальный daily-отчёт разделил нулевую работу отопления и один шестиминутный цикл ГВС.
- Два явно разрешённых OpenAI-вызова прошли strict Structured Output и evidence validation;
  в ledger записано 5,475 токенов. Один промежуточный вызов со старого RU deployment получил
  403 до генерации и не увеличил ledger.
- Последний полный локальный checkpoint: 57 тестов, Ruff, strict mypy, wheel и Compose config
  прошли; реальный `run --once` выполнил sync/catch-up/atomic publish, healthcheck вернул `ok`.

## Что реализовано

- Узкий ZONT adapter с allowlist только `devices` и `load_data`, recursive redaction секретов и
  ограничением частоты запросов.
- SQLite/WAL, canonical UPSERT, discovery/config snapshots, отчёты, evidence, lifecycle
  рекомендаций, outbox, usage ledger, integrity check и online backup.
- Автоматический переход к packaged Alembic head с проверенным backup перед изменением revision.
- Качество данных, time-weighted температура относительно исторической уставки, наружная
  температура, циклы горелки и безопасная атрибуция отклонений.
- Контекст отопительных режимов/расписаний и отдельное фактическое автоматическое летнее
  состояние контура.
- Взаимоисключающие OpenTherm-состояния `heating / dhw / concurrent-or-ambiguous / idle` без
  двойного засчёта одновременных флагов.
- Детерминированные эпизоды ГВС: историческая цель/режим, восстановление и перелёт, запрос и
  пауза отопления, возврат `ch`/worktime без пламени, горячий хвост и quality guards.
- Initial/daily/weekly/monthly/seasonal команды, безопасные renderers text/JSON/standalone HTML,
  локальный lifecycle рекомендаций и автономный worker с catch-up/heartbeat/atomic publish.
- OpenAI Responses API через строгую схему, `store=False`, без tools/functions, с проверкой
  evidence ID, budget и graceful degradation.
- Непривилегированный Docker image и CI с lint/types/tests/wheel import/Docker build.

## ГВС: реализованный вертикальный срез

В реальной БД уже есть отдельный контур «ГВС»: температура БКН `dt`, `mode_id`, `target_temp`,
`setpoint_temp`, `status`, `worktime`, температура подачи `bt` и OpenTherm-флаги `ch/dhw/fl`.
Системные режимы задают разные политики отопления и ГВС; автоматическое летнее состояние
отопления не означает отключение ГВС.

Анализ использует эти ряды совместно: исторические режим и цель, температура, status/worktime,
взаимоисключающие состояния `ch/dhw`, температура подачи и комнатный дефицит. Для каждого
эпизода он показывает наблюдавшиеся факты, выводы из нескольких сигналов и недоказанную
гидравлическую гипотезу; отсутствие подтверждённого запроса отопления не становится тревогой.

Подробные ряды ГВС накоплены только с `2026-07-22`; зимнего набора пока нет. Текущие config
snapshots нельзя ретроспективно применять ко всей истории, поэтому анализ обязан использовать
исторические `mode_id/target_temp` и явно маркировать неизвестный старый контекст.

## Главные незавершённые части

В порядке продуктового приоритета:

1. Провести 2–4 недели P2-пилота и собрать оценки владельца: верно/ложно/непонятно.
2. Исправить обнаруженные на пилоте пропуски данных, ложные тревоги и непонятные формулировки.
3. Baseline и настоящие нормализованные weekly/monthly/seasonal сравнения (P3).
4. Внешний backup, межпроцессный lease и AI eval-набор после подтверждения пользы.
5. Telegram, новые миграции и storage benchmark — только по подтверждённой необходимости.

## Ограничения, которые нельзя скрывать

- Анализ сейчас выбирает один основной отопительный контур/комнатный датчик.
- Нет прямого ряда насоса, трёхходового клапана, расхода ГВС и подтверждённой обратки. Можно
  наблюдать задержку переключения, но не всегда доказать её гидравлическую причину.
- Команды длинных периодов уже существуют, но пока не выполняют обещанные сравнения периодов.
- Межпроцессного lease пока нет; запускать следует ровно один worker-контейнер.

## Safety invariants

1. Не добавлять ZONT mutation endpoint, generic request или model tool для оборудования.
2. Не хранить и не печатать raw credentials; discovery всегда проходит recursive redaction.
3. Числа рассчитывает детерминированный код; AI только интерпретирует переданные evidence ID.
4. При плохом качестве данных разрешено советовать только восстановление наблюдаемости.
5. `mark-applied` фиксирует ручное действие владельца и никогда не обращается к ZONT.
6. Сохранять idempotent UPSERT и стабильные report/evidence IDs.

## Возобновление разработки

```bash
. .venv/bin/activate
pytest
ruff check .
mypy src/zont_analyzer
```

Перед функциональными изменениями проверить `git status` и сохранить текущую рабочую версию.
